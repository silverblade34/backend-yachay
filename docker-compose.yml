version: '3.8'
services:
  v1:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '0.0.0.0:3030:3030'
    env_file:
      - .env
    restart: unless-stopped
    container_name: backend-yachay-app
    environment:
      - PORT=3030