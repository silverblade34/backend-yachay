version: '3'
services:
  v1:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3030:3030'
    env_file:
      - .env
    restart: unless-stopped
    container_name: backend-yachay-app